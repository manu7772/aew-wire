<?php
namespace {{ entity.namespace }};

{% for use in entity.classUses %}
use {{ use }};
{% endfor %}

class {{ entity.shortname }}{{ entity.extends ? ' extends ' ~ entity.extends }}{{ entity.implements|length ? ' implements ' ~ entity.implements|join(', ') }}
{

{% if entity.constants|length %}
{% for name,value in entity.constants %}
    public const {{ name }} = {{ value|raw }};
{% endfor %}
{% endif %}
{% if entity.traits|length %}

    use {{ entity.traits|join(', ') }};
{% endif %}

}

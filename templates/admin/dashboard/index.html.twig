{% extends '@AequationWire/admin/base_admin.html.twig' %}

{% block title %}Admin | Tableau de bord{% endblock %}

{% block main_content %}
<h1 class="text-3xl font-semibold mb-4">{{ 'dashboard'|trans|capitalize }}</h1>
<div>PHP version <span class="font-bold">{{ app.phpInfo.VERSION }}</span></div>
<div>Symfony version <span class="font-bold">{{ app.symfonyInfo.VERSION }}</span></div>
{# {{ parent() }} #}
{% endblock %}

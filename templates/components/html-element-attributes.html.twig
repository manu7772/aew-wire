<html lang="{{ locale }}" data-theme="{{ app.csstheme }}" class="{{ app.user ? 'userlogged ' }}{{ app.csstheme }}" data-cssthemes="{{ app.currentCssthemes|json_encode }}" data-csstheme-switcher-url="{{ switcherUrl }}">

{# 

to define a theme instead of toggling it, set attribute data-csstheme-define="--theme_name--" on the element.

#}
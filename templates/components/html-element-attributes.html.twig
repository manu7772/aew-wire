<html lang="{{ locale }}" data-theme="{{ app.public ? 'light' : 'dark' }}" class="{{ app.user ? 'userlogged ' }}">
    
{# 

<html lang="{{ locale }}" data-theme="{{ app.csstheme }}" class="{{ app.user ? 'userlogged ' }}{{ app.csstheme }}">

-- to define a theme instead of toggling it, set attribute data-csstheme-define="--theme_name--" on the element.

#}
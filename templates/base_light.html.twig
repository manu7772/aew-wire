<!DOCTYPE html>
<html lang="{{ app.locale }}" class="{{ app.darkmodeClass }}">
	<head>
		<meta charset="{{ app.charset }}">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>{% block title %}Aequation Wire BUNDLE{% endblock %}</title>
		<link rel="icon" href="{% block favicon %}{{ asset(app.service.param('favicon.admin', 'favicon.svg')) }}{% endblock %}">
        {# <link rel="stylesheet" href="{{ asset('styles/pico.css') }}">
        <link rel="stylesheet" href="{{ asset('styles/pico.colors.css') }}">
        <link rel="stylesheet" href="{{ asset('styles/wire-simple.css') }}"> #}
		{% block javascripts %}
		{# {% block importmap %}{{ importmap('wire') }}{% endblock %} #}
		{% endblock %}
	</head>
	<body>
		<main class="container-fluid">
			{% block body %}
				{% block outer_title_h1 %}
				<h1>{% block title_h1 %}Documentation{% endblock %}</h1>
				{% endblock %}
				{% block page_content %}
				<div class="columns-container">
					<div class="left-col">
						{% block sub_menu %}
						{% if submenu is defined %}
						{% include "@AequationWire/bases/_submenu.html.twig" with {submenu: submenu} %}
						{% endif %}
						{% endblock %}
					</div>
					<div class="right-col">
						{% block right_content %}
						{% endblock %}
					</div>
				</div>
				{% endblock %}
			{% endblock %}
		</main>
		<footer>
			{% block footer %}
			{% endblock %}
		</footer>
		{% block after_container %}
			{# commands, modals, etc. content here #}
            {% include '@AequationWire/partials/_flashes.html.twig' %}
		{% endblock %}
	</body>
</html>
